node_modules/
dist/
dist-mobile/
*.log
.DS_Store
.vite/

# 打包产物（322MB，不该进仓库；由 npm run pack 本地生成）
release/

# 临时/缓存
.tmp-yuki/
.tmp-install/
.tmp-s/
.tmp-split/
.tmp-refs/
.tmp-photos/
.tmp-photo-refs/
*.tmp
# 无头浏览器验证时用的临时 profile
.tmp-chrome-profile/

# 素材换代时的备份目录（历史遗留的防呆规则，实际已清理干净）
.backup-*/

# 本地环境
.env
.env.*

# Tauri / Rust 本地产物（src-tauri/target 为 Rust 构建产物，数百 MB；
# gen/schemas 为 tauri-build 生成物，随构建再生）
src-tauri/target/
src-tauri/gen/

# Mimosa 安全扫描的本地工作目录
.mimosa/

# 预览脚本的临时启动器（被杀进程时会残留）
.tmp-open-mobile.cjs